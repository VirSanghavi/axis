# This repo is bun-managed: bun.lock is the only lockfile, CI installs with
# `bun install --frozen-lockfile`. If you run npm here anyway, this stops it
# from re-introducing a competing package-lock.json.
package-lock=false
